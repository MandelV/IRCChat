language: java
env:
  - CODACY_PROJECT_TOKEN=186524ef5d1f4268b3cf16e4fc135c5a
before_install:
  - sudo apt-get install ngircd
  - sudo apt-get install jq
  - wget -O ~/codacy-coverage-reporter-assembly-latest.jar $(curl https://api.github.com/repos/codacy/codacy-coverage-reporter/releases/latest | jq -r .assets[0].browser_download_url)

script:
  - mvn com.gavinmogan:codacy-maven-plugin:coverage -DcoverageReportFile=target/site/jacoco/jacoco.xml -DprojectToken=186524ef5d1f4268b3cf16e4fc135c5a -DapiToken=wFLZi81A9TcWRFFXI55E
after_success:
- cd target
- ls
- java -jar ~/codacy-coverage-reporter-assembly-latest.jar report -l Java -r target/site/jacoco/jacoco.xml
